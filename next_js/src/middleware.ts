export { default } from "next-auth/middleware";

export const config = {
    matcher: [
        // "/TabinoOtomo/(.*)",
        // "!/TabinoOtomo/login",
        // "!/auth/signin",
        // "!/"
      ],
};